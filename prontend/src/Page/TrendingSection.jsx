// components/TrendingSection.jsx
function TrendingSection(/*{ list }*/) {
  return (
    <section>
      <h2>🔥 인기 애니 TOP</h2>
      {/* <ol>
        {list.map((anime) => (
          <li key={anime.id}>
            {anime.title} ⭐ {anime.rating}
          </li>
        ))}
      </ol> */}
    </section>
  );
}

export default TrendingSection;
